import { ResourceListPage } from "@nextpaneljs/core/pages";
import { {{pascalCase name}}Resource } from "@/resources/{{lowercase name}}.resource";
import { queryRecords, deleteRecord } from "@/nextpanel/actions";

const resource = {{pascalCase name}}Resource;

export default async function {{pascalCase (pluralize name)}}ListPage({
  searchParams,
}: {
  searchParams: Promise<Record<string, string | string[] | undefined>>;
}) {
  return (
    <ResourceListPage
      resource={resource}
      searchParams={await searchParams}
      queryRecords={queryRecords}
      deleteRecord={deleteRecord}
    />
  );
}
